services:
  thunder-subtitle:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "3443:443"
    volumes:
      - /Users/mac/Documents/media:/media
      - ./data:/root
    environment:
      - ADMIN_PASSWORD=changeme
      - MEDIA_PATHS=/media
      - JWT_SECRET=change-me-in-production